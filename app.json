{
  "name": "CyberSecPro",
  "description": "WhatsApp Bot Management SaaS - link numbers via pairing codes, manage sessions from a cyberpunk dashboard.",
  "repository": "https://github.com/msarim21/cybersecweb",
  "keywords": [
    "node",
    "whatsapp",
    "saas",
    "bot",
    "baileys"
  ],
  "env": {
    "MONGO_URL": {
      "description": "MongoDB connection string - REQUIRED. Get a free cluster at mongodb.com/atlas",
      "required": true
    },
    "JWT_SECRET": {
      "description": "Secret key for signing JWT auth tokens.",
      "generator": "secret"
    },
    "APP_URL": {
      "description": "Your Heroku app URL - used for keepalive pings so bots stay online. Format: https://YOUR-APP-NAME.herokuapp.com",
      "required": false
    },
    "TELEGRAM_BOT_TOKEN": {
      "description": "Your Telegram bot token from @BotFather",
      "required": false
    },
    "ADMIN_EMAIL": {
      "description": "Email for the auto-created admin account.",
      "required": false
    },
    "ADMIN_PASSWORD": {
      "description": "Password for the auto-created admin account (min 6 chars).",
      "required": false
    },
    "NPM_CONFIG_PRODUCTION": {
      "description": "Must be false so devDependencies are installed for the React build step.",
      "value": "false"
    },
    "NPM_CONFIG_LEGACY_PEER_DEPS": {
      "description": "Use legacy peer deps resolution to avoid install conflicts.",
      "value": "true"
    },
    "NODE_ENV": {
      "description": "Runtime environment.",
      "value": "production"
    },
    "WHATSAPP_HOST_DYNO": {
      "description": "Which dyno type runs WhatsApp bots. 'web' = bots run on the web dyno (Eco plan friendly). No worker dyno needed.",
      "value": "web",
      "required": false
    },
    "BOT_ISOLATION": {
      "description": "Use isolated mode so the supervisor can rotate and recover bots one-by-one.",
      "value": "1",
      "required": false
    },
    "DYNO_TOTAL_RAM_MB": {
      "description": "Actual RAM ceiling of the dyno. Eco/Basic web dynos have 512MB.",
      "value": "512",
      "required": false
    },
    "BOT_CHILD_HEAP_MB": {
      "description": "V8 heap limit for each bot child process. Keep this lean on Eco dynos.",
      "value": "128",
      "required": false
    },
    "MAX_MEM_PERCENT": {
      "description": "Stop spawning new bot children once projected memory crosses this percent of the dyno limit.",
      "value": "80",
      "required": false
    },
    "MAX_BOTS_PER_DYNO": {
      "description": "Hard cap on bots per dyno. Leave empty for no limit.",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "formation": {
    "web": {
      "quantity": 1,
      "size": "eco"
    }
  },
  "scripts": {
    "postdeploy": "echo 'CyberSecPro deployed successfully'"
  },
  "stack": "heroku-26"
}
